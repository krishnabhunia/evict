<UserControl x:Class="Evict.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Evict.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}">

    <UserControl.Resources>
        <ContextMenu x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="Scan for leftovers again…" Command="{Binding RescanLeftoversCommand}" CommandParameter="{Binding SelectedItem}" />
            <MenuItem Header="Remove from history" Command="{Binding RemoveCommand}" CommandParameter="{Binding SelectedItem}" />
        </ContextMenu>
    </UserControl.Resources>

    <Grid Margin="24,18,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="History" Style="{StaticResource Text.Title}" />
                <TextBlock Text="{Binding Summary}" Style="{StaticResource Text.Subtitle}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <TextBox Style="{StaticResource TextBox.Search}" Width="240" Tag="Search history…" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}" />
                <Button Style="{StaticResource Button.Secondary}" Content="Export CSV" Command="{Binding ExportCommand}" Margin="8,0,0,0" />
                <Button Style="{StaticResource Button.Ghost}" Content="Clear" Command="{Binding ClearAllCommand}" Margin="8,0,0,0" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0" Margin="0,14,0,0">
            <Grid>
                <DataGrid Style="{StaticResource DataGrid.Flat}" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" ContextMenu="{StaticResource RowMenu}">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="When" Width="150" SortMemberPath="Entry.Timestamp">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate DataType="{x:Type vm:HistoryItemViewModel}"><TextBlock Text="{Binding When}" Style="{StaticResource Cell.Secondary}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Program" Width="*" MinWidth="200" SortMemberPath="Name">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:HistoryItemViewModel}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource Cell.Primary}" />
                                        <TextBlock Style="{StaticResource Cell.Secondary}" FontSize="11.5">
                                            <Run Text="{Binding Publisher, Mode=OneWay}" /><Run Text=" " /><Run Text="{Binding Version, Mode=OneWay}" />
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Method" Width="100" SortMemberPath="MethodText">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate DataType="{x:Type vm:HistoryItemViewModel}"><TextBlock Text="{Binding MethodText}" Style="{StaticResource Cell.Secondary}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Result" Width="110" SortMemberPath="Succeeded">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:HistoryItemViewModel}">
                                    <Border Style="{StaticResource Pill}" Background="{Binding Succeeded, Converter={StaticResource BoolToBrush}, ConverterParameter='Brush.Success.Subtle|Brush.Warning.Subtle'}">
                                        <TextBlock Text="{Binding ResultText}" Style="{StaticResource Pill.Text}" Foreground="{Binding Succeeded, Converter={StaticResource BoolToBrush}, ConverterParameter='Brush.Success|Brush.Warning'}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Leftovers" Width="90" SortMemberPath="Entry.LeftoversFound">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate DataType="{x:Type vm:HistoryItemViewModel}"><TextBlock Text="{Binding LeftoversText}" Style="{StaticResource Cell.Secondary}" ToolTip="removed / found" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Reclaimed" Width="100" SortMemberPath="Entry.BytesReclaimed">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate DataType="{x:Type vm:HistoryItemViewModel}"><TextBlock Text="{Binding ReclaimedText}" Style="{StaticResource Cell.Primary}" HorizontalAlignment="Right" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Notes" Width="1.2*" MinWidth="160">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate DataType="{x:Type vm:HistoryItemViewModel}"><TextBlock Text="{Binding Notes}" Style="{StaticResource Cell.Secondary}" ToolTip="{Binding Notes}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False" Visibility="{Binding HasItems, Converter={StaticResource InvBoolToVis}}">
                    <TextBlock Text="&#xE81C;" Style="{StaticResource Icon}" FontSize="36" Foreground="{DynamicResource Brush.Text.Tertiary}" />
                    <TextBlock Text="Nothing here yet" Style="{StaticResource Text.Section}" HorizontalAlignment="Center" Margin="0,10,0,4" Foreground="{DynamicResource Brush.Text.Secondary}" />
                    <TextBlock Text="Every uninstall, force removal and app removal is recorded here." Style="{StaticResource Text.Caption}" HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Style="{StaticResource Text.Caption}" Margin="0,10,0,0" Text="Tip: right-click an entry to scan for leftovers again – useful if a program was removed outside Evict or files were still in use." />
    </Grid>
</UserControl>
