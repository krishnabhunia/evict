<UserControl x:Class="Evict.App.Views.WindowsAppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Evict.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:WindowsAppsViewModel}">

    <UserControl.Resources>
        <ContextMenu x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="Open install folder" Command="{Binding OpenInstallFolderCommand}" CommandParameter="{Binding SelectedItem}" />
            <MenuItem Header="Open in Microsoft Store" Command="{Binding OpenInStoreCommand}" CommandParameter="{Binding SelectedItem}" />
            <MenuItem Header="Copy package full name" Command="{Binding CopyPackageNameCommand}" CommandParameter="{Binding SelectedItem}" />
        </ContextMenu>
    </UserControl.Resources>

    <Grid Margin="24,18,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Windows Apps" Style="{StaticResource Text.Title}" />
                <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,4,0,0" Text="Microsoft Store and pre-installed (UWP / MSIX) apps, including bloatware Windows ships with. Apps marked non-removable by Windows are greyed out." />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <TextBox Style="{StaticResource TextBox.Search}" Width="240" Tag="Search apps…" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}" />
                <Button Style="{StaticResource Button.Secondary}" Command="{Binding RefreshCommand}" Margin="8,0,0,0" Padding="10,7">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" Style="{StaticResource Icon}" FontSize="14" Foreground="{DynamicResource Brush.Text.Primary}" />
                        <TextBlock Text="Refresh" Margin="7,0,0,0" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,14,0,0">
            <ToggleButton Style="{StaticResource Button.Chip}" IsChecked="{Binding OnlyBloatware}" Content="Bloatware only" />
            <ToggleButton Style="{StaticResource Button.Chip}" IsChecked="{Binding ShowSystem}" Content="Show system apps" Margin="8,0,0,0" />
            <ToggleButton Style="{StaticResource Button.Chip}" IsChecked="{Binding ShowFrameworks}" Content="Show frameworks" Margin="8,0,0,0" />
            <Button Style="{StaticResource Button.Link}" Content="Select all bloatware" Command="{Binding SelectBloatwareCommand}" Margin="16,0,0,0" />
            <CheckBox IsChecked="{Binding AllUsers}" IsEnabled="{Binding IsElevated}" Content="All user accounts" Margin="24,0,0,0" ToolTip="List and remove apps for every user account (requires administrator)" />
            <CheckBox IsChecked="{Binding AlsoRemoveProvisioned}" IsEnabled="{Binding IsElevated}" Content="Also de-provision" Margin="16,0,0,0" ToolTip="Also remove the provisioned package so the app does not reappear for new user accounts (requires administrator)" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource Text.Caption}" Visibility="{Binding StatusText, Converter={StaticResource BoolToVis}}" />
            <ProgressBar IsIndeterminate="True" Width="220" HorizontalAlignment="Right" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0" Margin="0,8,0,0">
            <Grid>
                <DataGrid Style="{StaticResource DataGrid.Flat}" ItemsSource="{Binding View}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" ContextMenu="{StaticResource RowMenu}">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource DataGrid.Row}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRemovable}" Value="False">
                                    <Setter Property="Opacity" Value="0.55" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="44" CanUserResize="False" CanUserSort="False">
                            <DataGridTemplateColumn.Header>
                                <CheckBox IsChecked="{Binding DataContext.AllSelected, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}" IsThreeState="False" Margin="2,0,0,0" />
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}">
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsRemovable}" Margin="2,0,0,0" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="App" Width="*" MinWidth="220" SortMemberPath="Name">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}">
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" Style="{StaticResource Cell.Primary}" />
                                            <TextBlock Text="{Binding PackageName}" Style="{StaticResource Cell.Secondary}" FontSize="11" />
                                        </StackPanel>
                                        <Border Style="{StaticResource Pill}" Background="{DynamicResource Brush.Warning.Subtle}" Margin="10,0,0,0" Visibility="{Binding IsBloat, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="Bloatware" Style="{StaticResource Pill.Text}" Foreground="{DynamicResource Brush.Warning}" />
                                        </Border>
                                        <Border Style="{StaticResource Pill}" Margin="10,0,0,0" Visibility="{Binding IsRemovable, Converter={StaticResource InvBoolToVis}}">
                                            <TextBlock Text="Non-removable" Style="{StaticResource Pill.Text}" />
                                        </Border>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Publisher" Width="180" SortMemberPath="Publisher">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}"><TextBlock Text="{Binding Publisher}" Style="{StaticResource Cell.Secondary}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Version" Width="120" SortMemberPath="Version">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}"><TextBlock Text="{Binding Version}" Style="{StaticResource Cell.Secondary}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Type" Width="90" SortMemberPath="TypeText">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}"><TextBlock Text="{Binding TypeText}" Style="{StaticResource Cell.Secondary}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Size" Width="90" SortMemberPath="SizeSort">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}"><TextBlock Text="{Binding SizeText}" Style="{StaticResource Cell.Primary}" HorizontalAlignment="Right" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Status" Width="200" SortMemberPath="Status">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:AppxItemViewModel}"><TextBlock Text="{Binding Status}" Style="{StaticResource Cell.Secondary}" ToolTip="{Binding Status}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="520" Visibility="{Binding Error, Converter={StaticResource BoolToVis}}" Background="{DynamicResource Brush.Danger.Subtle}" CornerRadius="8" Padding="16">
                    <TextBlock Text="{Binding Error}" Style="{StaticResource Text.Body}" />
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Summary}" Style="{StaticResource Text.Caption}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Style="{StaticResource Button.Danger}" Command="{Binding RemoveSelectedCommand}" MinWidth="200" Padding="18,9">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE74D;" Style="{StaticResource Icon}" FontSize="15" Foreground="White" />
                    <TextBlock Margin="9,0,0,0" Text="{Binding SelectedCount, Converter={StaticResource CountLabel}, ConverterParameter='Remove selected|Remove {0} app|Remove {0} apps'}" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
