<UserControl x:Class="Evict.App.Views.BrowserExtensionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Evict.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:BrowserExtensionsViewModel}">

    <UserControl.Resources>
        <ContextMenu x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="Show in browser's extension page" Command="{Binding OpenExtensionsPageCommand}" CommandParameter="{Binding SelectedItem}" />
            <MenuItem Header="Open extension folder" Command="{Binding OpenFolderCommand}" CommandParameter="{Binding SelectedItem}" />
            <MenuItem Header="Homepage / store listing" Command="{Binding OpenHomepageCommand}" CommandParameter="{Binding SelectedItem}" />
        </ContextMenu>
    </UserControl.Resources>

    <Grid Margin="24,18,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Browser Extensions" Style="{StaticResource Text.Title}" />
                <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,4,0,0" Text="Extensions and add-ons in Chrome, Edge, Brave, Vivaldi, Opera and Firefox – across every profile. Removal requires the browser to be closed." />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <TextBox Style="{StaticResource TextBox.Search}" Width="240" Tag="Search extensions…" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}" />
                <ToggleButton Style="{StaticResource Button.Chip}" IsChecked="{Binding ShowBuiltIn}" Content="Show built-in" Margin="8,0,0,0" MinHeight="34" />
                <Button Style="{StaticResource Button.Secondary}" Command="{Binding RefreshCommand}" Margin="8,0,0,0" Padding="10,7">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" Style="{StaticResource Icon}" FontSize="14" Foreground="{DynamicResource Brush.Text.Primary}" />
                        <TextBlock Text="Refresh" Margin="7,0,0,0" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource Text.Caption}" />
            <ProgressBar IsIndeterminate="True" Width="220" HorizontalAlignment="Right" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0" Margin="0,8,0,0">
            <Grid>
                <ListView Style="{StaticResource ListView.Flat}" ItemsSource="{Binding View}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" ContextMenu="{StaticResource RowMenu}">
                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.ContainerStyle>
                                <Style TargetType="GroupItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="GroupItem">
                                                <Expander Style="{StaticResource Expander.Group}" IsExpanded="True">
                                                    <Expander.Header>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="&#xE774;" Style="{StaticResource Icon}" FontSize="13" Margin="0,0,8,0" />
                                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}" />
                                                            <Border Style="{StaticResource Badge}"><TextBlock Text="{Binding ItemCount}" Style="{StaticResource Badge.Text}" /></Border>
                                                        </StackPanel>
                                                    </Expander.Header>
                                                    <ItemsPresenter Margin="0,0,0,4" />
                                                </Expander>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </GroupStyle.ContainerStyle>
                        </GroupStyle>
                    </ListView.GroupStyle>
                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{StaticResource GridViewHeader}">
                            <GridViewColumn Width="40">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}">
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanRemove}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Extension" Width="340">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}">
                                        <StackPanel ToolTip="{Binding Tooltip}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" Style="{StaticResource Cell.Primary}" />
                                                <Border Style="{StaticResource Pill}" Background="{DynamicResource Brush.Warning.Subtle}" Margin="8,0,0,0" Visibility="{Binding IsRisky, Converter={StaticResource BoolToVis}}" ToolTip="Requests broad permissions (all sites, browsing history, cookies…)">
                                                    <TextBlock Text="Broad permissions" Style="{StaticResource Pill.Text}" Foreground="{DynamicResource Brush.Warning}" />
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description}" Style="{StaticResource Cell.Secondary}" FontSize="11.5" Visibility="{Binding Description, Converter={StaticResource BoolToVis}}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Version" Width="100">
                                <GridViewColumn.CellTemplate><DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}"><TextBlock Text="{Binding Version}" Style="{StaticResource Cell.Secondary}" /></DataTemplate></GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="State" Width="80">
                                <GridViewColumn.CellTemplate><DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}"><TextBlock Text="{Binding StateText}" Style="{StaticResource Cell.Secondary}" /></DataTemplate></GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Source" Width="100">
                                <GridViewColumn.CellTemplate><DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}"><TextBlock Text="{Binding Source}" Style="{StaticResource Cell.Secondary}" /></DataTemplate></GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Installed" Width="110">
                                <GridViewColumn.CellTemplate><DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}"><TextBlock Text="{Binding InstalledText}" Style="{StaticResource Cell.Secondary}" /></DataTemplate></GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Size" Width="80">
                                <GridViewColumn.CellTemplate><DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}"><TextBlock Text="{Binding SizeText}" Style="{StaticResource Cell.Primary}" HorizontalAlignment="Right" Margin="0,0,8,0" /></DataTemplate></GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Status" Width="220">
                                <GridViewColumn.CellTemplate><DataTemplate DataType="{x:Type vm:ExtensionItemViewModel}"><TextBlock Text="{Binding Status}" Style="{StaticResource Cell.Secondary}" ToolTip="{Binding Status}" /></DataTemplate></GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False" Visibility="{Binding Count, Converter={StaticResource InvBoolToVis}}">
                    <TextBlock Text="&#xEA86;" Style="{StaticResource Icon}" FontSize="36" Foreground="{DynamicResource Brush.Text.Tertiary}" />
                    <TextBlock Text="No extensions found" Style="{StaticResource Text.Section}" HorizontalAlignment="Center" Margin="0,10,0,4" Foreground="{DynamicResource Brush.Text.Secondary}" />
                    <TextBlock Text="Evict looks in each browser's profile folders under your user account." Style="{StaticResource Text.Caption}" HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Summary}" Style="{StaticResource Text.Caption}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Style="{StaticResource Button.Danger}" Command="{Binding RemoveSelectedCommand}" MinWidth="200" Padding="18,9">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE74D;" Style="{StaticResource Icon}" FontSize="15" Foreground="White" />
                    <TextBlock Margin="9,0,0,0" Text="{Binding SelectedCount, Converter={StaticResource CountLabel}, ConverterParameter='Remove selected|Remove {0} extension|Remove {0} extensions'}" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
