<UserControl x:Class="Evict.App.Views.LeftoverReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Evict.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LeftoverReviewViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Border Style="{StaticResource Pill}" Background="{DynamicResource Brush.Accent.Subtle}">
                    <TextBlock Style="{StaticResource Pill.Text}" Foreground="{DynamicResource Brush.Accent}" Text="{Binding FileCount, Converter={StaticResource CountLabel}, ConverterParameter='file item|file items'}" />
                </Border>
                <Border Style="{StaticResource Pill}" Margin="6,0,0,0">
                    <TextBlock Style="{StaticResource Pill.Text}" Text="{Binding RegistryCount, Converter={StaticResource CountLabel}, ConverterParameter='registry item|registry items'}" />
                </Border>
                <Border Style="{StaticResource Pill}" Margin="6,0,0,0" Visibility="{Binding OtherCount, Converter={StaticResource BoolToVis}}">
                    <TextBlock Style="{StaticResource Pill.Text}" Text="{Binding OtherCount, Converter={StaticResource CountLabel}, ConverterParameter='service / task|services / tasks'}" />
                </Border>
                <TextBlock Text="{Binding TotalBytes, Converter={StaticResource Bytes}}" Style="{StaticResource Text.Caption}" VerticalAlignment="Center" Margin="12,0,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource Button.Link}" Content="Select all" Command="{Binding SelectAllCommand}" />
                <TextBlock Text="·" Margin="8,0" Foreground="{DynamicResource Brush.Text.Tertiary}" VerticalAlignment="Center" />
                <Button Style="{StaticResource Button.Link}" Content="Safe only" Command="{Binding SelectSafeOnlyCommand}" ToolTip="Select everything except items marked 'Review'" />
                <TextBlock Text="·" Margin="8,0" Foreground="{DynamicResource Brush.Text.Tertiary}" VerticalAlignment="Center" />
                <Button Style="{StaticResource Button.Link}" Content="None" Command="{Binding SelectNoneCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <ListView Style="{StaticResource ListView.Flat}" ItemsSource="{Binding View}" SelectionMode="Single">
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.ContainerStyle>
                            <Style TargetType="GroupItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GroupItem">
                                            <Expander Style="{StaticResource Expander.Group}" IsExpanded="True">
                                                <Expander.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}" />
                                                        <Border Style="{StaticResource Badge}">
                                                            <TextBlock Text="{Binding ItemCount}" Style="{StaticResource Badge.Text}" />
                                                        </Border>
                                                    </StackPanel>
                                                </Expander.Header>
                                                <ItemsPresenter Margin="0,0,0,4" />
                                            </Expander>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GroupStyle.ContainerStyle>
                    </GroupStyle>
                </ListView.GroupStyle>
                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource GridViewHeader}">
                        <GridViewColumn Width="40">
                            <GridViewColumn.Header>
                                <CheckBox IsChecked="{Binding DataContext.AllSelected, RelativeSource={RelativeSource AncestorType=ListView}, Mode=TwoWay}" IsThreeState="False" />
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:LeftoverItemViewModel}">
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Type" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:LeftoverItemViewModel}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding KindGlyph}" Style="{StaticResource Icon}" FontSize="13" Margin="0,0,7,0" />
                                        <TextBlock Text="{Binding KindText}" Style="{StaticResource Cell.Secondary}" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Location" Width="520">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:LeftoverItemViewModel}">
                                    <StackPanel ToolTip="{Binding Tooltip}">
                                        <TextBlock Text="{Binding Path}" Style="{StaticResource Cell.Primary}" />
                                        <TextBlock Text="{Binding Detail}" Style="{StaticResource Cell.Secondary}" FontSize="11.5" Visibility="{Binding Detail, Converter={StaticResource BoolToVis}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Size" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:LeftoverItemViewModel}">
                                    <TextBlock Text="{Binding SizeText}" Style="{StaticResource Cell.Primary}" HorizontalAlignment="Right" Margin="0,0,8,0" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Confidence" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:LeftoverItemViewModel}">
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource Pill}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHigh}" Value="True"><Setter Property="Background" Value="{DynamicResource Brush.Success.Subtle}" /></DataTrigger>
                                                    <DataTrigger Binding="{Binding IsLow}" Value="True"><Setter Property="Background" Value="{DynamicResource Brush.Warning.Subtle}" /></DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding ConfidenceText}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource Pill.Text}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHigh}" Value="True"><Setter Property="Foreground" Value="{DynamicResource Brush.Success}" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding IsLow}" Value="True"><Setter Property="Foreground" Value="{DynamicResource Brush.Warning}" /></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type vm:LeftoverItemViewModel}">
                                    <Button Style="{StaticResource Button.Ghost}" Content="&#xE8A7;" FontFamily="{StaticResource Font.Icons}" FontSize="12" Padding="6,4" ToolTip="Open location"
                                            Command="{Binding OpenCommand}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
