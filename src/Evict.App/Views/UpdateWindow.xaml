<Window x:Class="Evict.App.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Evict.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:UpdateViewModel}"
        Style="{StaticResource Window.Dialog}"
        Title="Update Evict" Width="640" Height="560" MinWidth="520" MinHeight="420"
        Icon="pack://application:,,,/Assets/app.ico">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
            <Image Source="pack://application:,,,/Assets/app-256.png" Width="44" Height="44" VerticalAlignment="Top" RenderOptions.BitmapScalingMode="HighQuality" />
            <StackPanel Margin="14,0,0,0">
                <TextBlock Text="{Binding Title}" Style="{StaticResource Text.Title}" />
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource Text.Subtitle}" Margin="0,4,0,0" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="What's new" Style="{StaticResource Text.Section}" Margin="16,12,16,6" />
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16,0,16,12">
                    <TextBlock Text="{Binding Notes}" Style="{StaticResource Text.Body}" TextWrapping="Wrap" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource Brush.Info.Subtle}" CornerRadius="8" Padding="14,10" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock Text="&#xE946;" Style="{StaticResource Icon}" FontSize="16" Foreground="{DynamicResource Brush.Accent}" DockPanel.Dock="Left" Margin="0,1,10,0" VerticalAlignment="Top" />
                <TextBlock Text="{Binding ModeText}" Style="{StaticResource Text.Body}" TextWrapping="Wrap" />
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <Grid Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <ProgressBar Grid.Row="0" Height="8" Value="{Binding Progress, Mode=OneWay}" Maximum="100" IsIndeterminate="{Binding IsIndeterminate}" />
                <TextBlock Grid.Row="1" Text="{Binding StatusText}" Style="{StaticResource Text.Caption}" Margin="0,6,0,0" />
            </Grid>
            <Border Background="{DynamicResource Brush.Danger.Subtle}" CornerRadius="8" Padding="12,8" Visibility="{Binding Error, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding Error}" Style="{StaticResource Text.Body}" TextWrapping="Wrap" Foreground="{DynamicResource Brush.Danger}" />
            </Border>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource Button.Link}" Content="Open release page" Command="{Binding OpenReleasePageCommand}" />
                <Button Style="{StaticResource Button.Link}" Content="Skip this version" Command="{Binding SkipVersionCommand}" Margin="16,0,0,0" IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource Button.Secondary}" Content="Later" Command="{Binding LaterCommand}" MinWidth="100" Margin="0,0,10,0" IsCancel="True" />
                <Button Style="{StaticResource Button.Primary}" Content="Download and install" Command="{Binding InstallCommand}" MinWidth="170" IsDefault="True"
                        Visibility="{Binding CanInstall, Converter={StaticResource BoolToVis}}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
