<Window x:Class="Evict.App.Views.StartupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Evict.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:StartupViewModel}"
        Style="{StaticResource Window.Dialog}"
        Title="Startup Apps" Width="900" Height="600" MinWidth="720" MinHeight="460"
        Icon="pack://application:,,,/Assets/app.ico">

    <Window.Resources>
        <ContextMenu x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="Open file location" Command="{Binding OpenLocationCommand}" CommandParameter="{Binding SelectedItem}" />
            <MenuItem Header="What is this? (web search)" Command="{Binding SearchWebCommand}" CommandParameter="{Binding SelectedItem}" />
            <Separator />
            <MenuItem Header="Remove from startup" Command="{Binding DeleteCommand}" CommandParameter="{Binding SelectedItem}" />
        </ContextMenu>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Startup Apps" Style="{StaticResource Text.Title}" />
                <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,4,0,0" Text="Programs that launch when you sign in – from the Run registry keys and the Startup folders. Switch them off (the same switch Task Manager uses) or remove the entry entirely." />
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource Button.Secondary}" Command="{Binding RefreshCommand}" VerticalAlignment="Top" Padding="10,7">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72C;" Style="{StaticResource Icon}" FontSize="14" Foreground="{DynamicResource Brush.Text.Primary}" />
                    <TextBlock Text="Refresh" Margin="7,0,0,0" />
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource DataGrid.Flat}" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" ContextMenu="{StaticResource RowMenu}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="On" Width="60" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type vm:StartupItemViewModel}">
                                <CheckBox IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanToggle}" HorizontalAlignment="Center" ToolTip="Enable / disable at sign-in" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Name" Width="200" SortMemberPath="Name">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type vm:StartupItemViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource Cell.Primary}" />
                                    <Border Style="{StaticResource Pill}" Background="{DynamicResource Brush.Warning.Subtle}" Margin="8,0,0,0" Visibility="{Binding TargetMissing, Converter={StaticResource BoolToVis}}" ToolTip="The file this entry points to no longer exists">
                                        <TextBlock Text="File missing" Style="{StaticResource Pill.Text}" Foreground="{DynamicResource Brush.Warning}" />
                                    </Border>
                                    <TextBlock Text="&#xE7EF;" Style="{StaticResource Icon}" FontSize="11" Margin="8,0,0,0" Foreground="{DynamicResource Brush.Text.Tertiary}" ToolTip="All-users entry – administrator rights required to change" Visibility="{Binding RequiresAdmin, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Command" Width="*" SortMemberPath="Command">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type vm:StartupItemViewModel}"><TextBlock Text="{Binding Command}" Style="{StaticResource Cell.Secondary}" ToolTip="{Binding Command}" FontFamily="{StaticResource Font.Mono}" FontSize="11.5" /></DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Location" Width="200" SortMemberPath="LocationText">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type vm:StartupItemViewModel}"><TextBlock Text="{Binding LocationText}" Style="{StaticResource Cell.Secondary}" /></DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Status" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type vm:StartupItemViewModel}"><TextBlock Text="{Binding Status}" Style="{StaticResource Cell.Secondary}" /></DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Summary}" Style="{StaticResource Text.Caption}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource Button.Ghost}" Content="Remove selected" Command="{Binding DeleteCommand}" Margin="0,0,10,0" />
                <Button Style="{StaticResource Button.Primary}" Content="Close" Command="{Binding CloseCommand}" MinWidth="110" IsCancel="True" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
