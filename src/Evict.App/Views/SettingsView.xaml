<UserControl x:Class="Evict.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Evict.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}">

    <UserControl.Resources>
        <Style x:Key="SettingRow" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>
        <Style x:Key="Hint" TargetType="TextBlock" BasedOn="{StaticResource Text.Caption}">
            <Setter Property="Margin" Value="26,-8,0,12" />
        </Style>
        <Style x:Key="Num" TargetType="TextBox" BasedOn="{StaticResource TextBox.Base}">
            <Setter Property="Width" Value="90" />
            <Setter Property="Margin" Value="12,0,0,0" />
            <Setter Property="TextAlignment" Value="Right" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,18,24,24" MaxWidth="820" HorizontalAlignment="Left">
            <TextBlock Text="Settings" Style="{StaticResource Text.Title}" />
            <TextBlock Text="Changes are saved immediately." Style="{StaticResource Text.Subtitle}" Margin="0,4,0,16" />

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Appearance" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding IsDarkTheme}" Content="Dark theme" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="Text size" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="0,0,12,0" />
                        <ComboBox ItemsSource="{Binding TextSizeOptions}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding TextSize, Mode=TwoWay}" Width="200" />
                        <TextBlock Text="Ctrl + / Ctrl − / Ctrl 0 also work anywhere in the app." Style="{StaticResource Text.Caption}" VerticalAlignment="Center" Margin="12,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Uninstalling" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding CreateRestorePoint}" Content="Create a System Restore point before uninstalling (needs administrator)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding QuietUninstall}" Content="Prefer silent uninstall when the installer supports it" />
                    <TextBlock Style="{StaticResource Hint}" Text="MSI, Inno Setup, NSIS and InstallShield uninstallers can run without showing their own wizard. Others still show it." />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding AutoCleanLeftovers}" Content="Remove leftovers automatically after the uninstaller finishes (skip the review step)" />
                    <TextBlock Style="{StaticResource Hint}" Text="Only 'Safe' and 'Likely' items are removed automatically – 'Review' items are always left for you to decide." />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding SendToRecycleBin}" Content="Send leftover files and folders to the Recycle Bin instead of deleting permanently" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ScanAllUserProfiles}" Content="Scan every user account's AppData for leftovers (needs administrator)" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Program list" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding MeasureFolderSizes}" Content="Measure install folder sizes when the installer did not report one (slower first load)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ShowSystemComponents}" Content="Show hidden system components (runtimes, redistributables, drivers)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ShowWindowsUpdatesInPrograms}" Content="Show updates and hotfixes in the program list" />
                    <Border Style="{StaticResource Divider}" Margin="0,4,0,14" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="'Recently Installed' means installed within the last" Style="{StaticResource Text.Body}" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource Num}" Text="{Binding RecentlyInstalledDays, UpdateSourceTrigger=LostFocus}" />
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="days" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="8,0,0,0" />

                        <TextBlock Grid.Row="1" Text="'Large Programs' are at least" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource Num}" Text="{Binding LargeProgramThresholdMb, UpdateSourceTrigger=LostFocus}" Margin="12,10,0,0" />
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="MB" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="8,10,0,0" />

                        <TextBlock Grid.Row="2" Text="'Infrequently Used' means not launched for" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource Num}" Text="{Binding InfrequentlyUsedDays, UpdateSourceTrigger=LostFocus}" Margin="12,10,0,0" />
                        <TextBlock Grid.Row="2" Grid.Column="2" Text="days" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="8,10,0,0" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Integration" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ExplorerContextMenu}" Content="Add 'Uninstall with Evict' to the right-click menu of programs and shortcuts in Explorer" />
                    <TextBlock Style="{StaticResource Hint}" Text="Registered for your user account only (no administrator needed). If you move Evict.exe, toggle this off and on again." />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding HealthAutoScan}" Content="Run the Software Health scan automatically when Evict starts" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Notification area &amp; background" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ShowTrayIcon}" Content="Show the Evict icon in the notification area (system tray)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding CloseToTray}" Content="Closing the window keeps Evict running in the notification area" Margin="26,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding MinimizeToTray}" Content="Minimizing hides the window to the notification area" Margin="26,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding StartWithWindows}" Content="Start Evict with Windows (hidden in the notification area)" />
                    <TextBlock Style="{StaticResource Hint}" Text="Adds an entry for your user account under Settings → Apps → Startup; no administrator needed." />
                    <Border Style="{StaticResource Divider}" Margin="0,4,0,14" />
                    <TextBlock Text="When a program installer starts" Style="{StaticResource Text.Body}" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <ComboBox ItemsSource="{Binding InstallerDetectionOptions}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding InstallerDetection, Mode=TwoWay}" Width="420" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding DetectionStatusText}" Style="{StaticResource Text.Caption}" Margin="0,8,0,0" TextWrapping="Wrap" />
                    <TextBlock Style="{StaticResource Text.Caption}" Margin="0,4,0,0" TextWrapping="Wrap"
                               Text="Evict recognises setup.exe / *install*.exe files, Windows Installer packages (msiexec /i) and the temporary stubs of common installer frameworks. Recording starts at detection, so the first second or two of an installation may not be captured." />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Scheduled Software Health scan" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Run a scan" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="0,0,10,0" />
                        <ComboBox ItemsSource="{Binding ScheduledScanOptions}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding ScheduledScan, Mode=TwoWay}" Width="150" />
                        <TextBlock Text="on" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="10,0,10,0" Visibility="{Binding IsWeeklyScan, Converter={StaticResource BoolToVis}}" />
                        <ComboBox ItemsSource="{Binding WeekdayOptions}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding ScheduledScanWeekday, Mode=TwoWay}" Width="140" Visibility="{Binding IsWeeklyScan, Converter={StaticResource BoolToVis}}" />
                        <TextBlock Text="at" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="10,0,10,0" Visibility="{Binding IsScheduledScanOn, Converter={StaticResource BoolToVis}}" />
                        <ComboBox ItemsSource="{Binding HourOptions}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding ScheduledScanHour, Mode=TwoWay}" Width="110" Visibility="{Binding IsScheduledScanOn, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                    <TextBlock Text="{Binding ScheduleStatusText}" Style="{StaticResource Text.Caption}" Margin="0,10,0,0" TextWrapping="Wrap" />
                    <TextBlock Style="{StaticResource Text.Caption}" Margin="0,4,0,0" TextWrapping="Wrap"
                               Text="Uses Windows Task Scheduler for your user account (no administrator needed). If the PC is off at that time the scan runs at the next start of Evict instead." />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Updates" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding CheckForUpdates}" Content="Check for a new version on GitHub when Evict starts" />
                    <TextBlock Style="{StaticResource Hint}" Text="{Binding EditionText}" />
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource Button.Secondary}" Content="Check now" Command="{Binding CheckForUpdatesNowCommand}" IsEnabled="{Binding IsCheckingForUpdates, Converter={StaticResource InvBool}}" />
                        <Button Style="{StaticResource Button.Primary}" Content="Install update" Command="{Binding ShowUpdateCommand}" Margin="8,0,0,0" Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVis}}" />
                        <Button Style="{StaticResource Button.Link}" Content="All releases on GitHub" Command="{Binding OpenReleasesCommand}" Margin="8,0,0,0" />
                        <ProgressBar IsIndeterminate="True" Width="120" Margin="12,0,0,0" VerticalAlignment="Center" Visibility="{Binding IsCheckingForUpdates, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                    <TextBlock Text="{Binding UpdateStatusText}" Style="{StaticResource Text.Caption}" Margin="0,10,0,0" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="About" Style="{StaticResource Text.Section}" Margin="0,0,0,10" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Image Source="pack://application:,,,/Assets/app-256.png" Width="40" Height="40" RenderOptions.BitmapScalingMode="HighQuality" />
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Evict Uninstaller" Style="{StaticResource Text.Body}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding VersionText}" Style="{StaticResource Text.Caption}" />
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding ElevationText}" Style="{StaticResource Text.Caption}" />
                    <TextBlock Text="{Binding RuntimeText}" Style="{StaticResource Text.Caption}" Margin="0,2,0,0" />
                    <TextBlock Style="{StaticResource Text.Caption}" Margin="0,2,0,10">
                        <Run Text="Data folder: " /><Run Text="{Binding DataFolder, Mode=OneWay}" />
                    </TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource Button.Secondary}" Content="Open data folder" Command="{Binding OpenDataFolderCommand}" />
                        <Button Style="{StaticResource Button.Secondary}" Content="Open log file" Command="{Binding OpenLogFileCommand}" Margin="8,0,0,0" />
                        <Button Style="{StaticResource Button.Ghost}" Content="Reset to defaults" Command="{Binding ResetDefaultsCommand}" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
