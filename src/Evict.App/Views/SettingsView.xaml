<UserControl x:Class="Evict.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Evict.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}">

    <UserControl.Resources>
        <Style x:Key="SettingRow" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>
        <Style x:Key="Hint" TargetType="TextBlock" BasedOn="{StaticResource Text.Caption}">
            <Setter Property="Margin" Value="26,-8,0,12" />
        </Style>
        <Style x:Key="Num" TargetType="TextBox" BasedOn="{StaticResource TextBox.Base}">
            <Setter Property="Width" Value="90" />
            <Setter Property="Margin" Value="12,0,0,0" />
            <Setter Property="TextAlignment" Value="Right" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,18,24,24" MaxWidth="820" HorizontalAlignment="Left">
            <TextBlock Text="Settings" Style="{StaticResource Text.Title}" />
            <TextBlock Text="Changes are saved immediately." Style="{StaticResource Text.Subtitle}" Margin="0,4,0,16" />

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Appearance" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding IsDarkTheme}" Content="Dark theme" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Uninstalling" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding CreateRestorePoint}" Content="Create a System Restore point before uninstalling (needs administrator)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding QuietUninstall}" Content="Prefer silent uninstall when the installer supports it" />
                    <TextBlock Style="{StaticResource Hint}" Text="MSI, Inno Setup, NSIS and InstallShield uninstallers can run without showing their own wizard. Others still show it." />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding AutoCleanLeftovers}" Content="Remove leftovers automatically after the uninstaller finishes (skip the review step)" />
                    <TextBlock Style="{StaticResource Hint}" Text="Only 'Safe' and 'Likely' items are removed automatically – 'Review' items are always left for you to decide." />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding SendToRecycleBin}" Content="Send leftover files and folders to the Recycle Bin instead of deleting permanently" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ScanAllUserProfiles}" Content="Scan every user account's AppData for leftovers (needs administrator)" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Program list" Style="{StaticResource Text.Section}" Margin="0,0,0,12" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding MeasureFolderSizes}" Content="Measure install folder sizes when the installer did not report one (slower first load)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ShowSystemComponents}" Content="Show hidden system components (runtimes, redistributables, drivers)" />
                    <CheckBox Style="{StaticResource SettingRow}" IsChecked="{Binding ShowWindowsUpdatesInPrograms}" Content="Show updates and hotfixes in the program list" />
                    <Border Style="{StaticResource Divider}" Margin="0,4,0,14" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="'Recently Installed' means installed within the last" Style="{StaticResource Text.Body}" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource Num}" Text="{Binding RecentlyInstalledDays, UpdateSourceTrigger=LostFocus}" />
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="days" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="8,0,0,0" />

                        <TextBlock Grid.Row="1" Text="'Large Programs' are at least" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource Num}" Text="{Binding LargeProgramThresholdMb, UpdateSourceTrigger=LostFocus}" Margin="12,10,0,0" />
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="MB" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="8,10,0,0" />

                        <TextBlock Grid.Row="2" Text="'Infrequently Used' means not launched for" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource Num}" Text="{Binding InfrequentlyUsedDays, UpdateSourceTrigger=LostFocus}" Margin="12,10,0,0" />
                        <TextBlock Grid.Row="2" Grid.Column="2" Text="days" Style="{StaticResource Text.Body}" VerticalAlignment="Center" Margin="8,10,0,0" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="About" Style="{StaticResource Text.Section}" Margin="0,0,0,10" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Image Source="pack://application:,,,/Assets/app-256.png" Width="40" Height="40" RenderOptions.BitmapScalingMode="HighQuality" />
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Evict Uninstaller" Style="{StaticResource Text.Body}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding VersionText}" Style="{StaticResource Text.Caption}" />
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding ElevationText}" Style="{StaticResource Text.Caption}" />
                    <TextBlock Text="{Binding RuntimeText}" Style="{StaticResource Text.Caption}" Margin="0,2,0,0" />
                    <TextBlock Style="{StaticResource Text.Caption}" Margin="0,2,0,10">
                        <Run Text="Data folder:" /><Run Text="{Binding DataFolder, Mode=OneWay}" />
                    </TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource Button.Secondary}" Content="Open data folder" Command="{Binding OpenDataFolderCommand}" />
                        <Button Style="{StaticResource Button.Secondary}" Content="Open log file" Command="{Binding OpenLogFileCommand}" Margin="8,0,0,0" />
                        <Button Style="{StaticResource Button.Ghost}" Content="Reset to defaults" Command="{Binding ResetDefaultsCommand}" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
